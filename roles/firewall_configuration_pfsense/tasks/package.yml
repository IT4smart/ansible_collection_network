# code: language=ansible
- name: package | Install packages
  ansible.builtin.package:
    name: "{{ item.name }}"
    state: "{{ item.state | default('present') }}"
  become: true
  loop: "{{ firewall_configuration_pfsense_packages }}"
  loop_control:
    label: "{{ item.name }}"
